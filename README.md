# Мобильное приложение "Мои шаги"

Простое Android-приложение для подсчета шагов с использованием встроенного датчика шагов и визуализацией статистики.

## 📱 Функциональность

### Основные возможности:
- **Подсчет шагов в реальном времени** с использованием датчика шагов устройства
- **Сохранение истории шагов** в локальной базе данных SQLite
- **Визуализация статистики** в виде гистограммы за последние 7 дней
- **Автоматический сброс счетчика** при смене дня
- **Два основных экрана** с навигацией через Bottom Navigation:
    - Главный экран с текущим количеством шагов
    - Экран истории с графиком и статистикой

### Особенности:
- Запрос разрешения на отслеживание активности
- Возможность сброса шагов (долгое нажатие на счетчик)
- Автоматическое определение смены дня
- Отображение статистики за неделю с названиями дней

## 🏗️ Архитектура

### Структура проекта:
```
MainActivity (Контейнер фрагментов)
├── HomeFragment (Экран подсчета шагов)
└── HistoryFragment (Экран истории)
```

### Ключевые компоненты:

1. **MainActivity** - основной контейнер с Bottom Navigation
2. **HomeFragment** - отображает текущее количество шагов, работает с датчиком
3. **HistoryFragment** - показывает историю шагов и гистограмму
4. **DBHelper** - управление локальной базой данных SQLite
5. **BarChartView** - кастомный View для отрисовки гистограммы
6. **DayChanger** (BroadcastReceiver) - обработка смены дня и перезагрузки устройства

## 🛠️ Технологии

- **Язык:** Java
- **Минимальная версия Android:** API 28 (Android 5.0)
- **База данных:** SQLite с SQLiteOpenHelper
- **Датчики:** Sensor.TYPE_STEP_COUNTER
- **Разрешения:** ACTIVITY_RECOGNITION

## 📁 Структура файлов

```
app/
├── src/main/java/com/example/scapplication/
│   ├── MainActivity.java          # Основная активность
│   ├── HomeFragment.java          # Фрагмент подсчета шагов
│   ├── HistoryFragment.java       # Фрагмент истории
│   ├── DBHelper.java              # Помощник базы данных
│   ├── BarChartView.java          # Кастомный View для диаграммы
│   └── DayChanger.java            # Обработка смены дня
├── src/main/res/layout/
│   ├── activity_main.xml          # Основной layout
│     ├── fragment_home.xml          # Layout главного экрана
│   └── fragment_history.xml       # Layout истории
└── src/main/res/menu/
    └── bottom_nav.xml             # Меню навигации
```

## 🔧 Настройка и запуск

### Требования:
- Android Studio 4.0+
- Устройство с датчиком шагов или эмулятор
- Для Android Q (API 29) и выше требуется разрешение ACTIVITY_RECOGNITION

### Установка:
1. Клонируйте репозиторий
2. Откройте проект в Android Studio
3. Дождитесь завершения синхронизации Gradle
4. Запустите на устройстве или эмуляторе

### Разрешения:
При первом запуске на Android Q+ приложение запросит разрешение на отслеживание активности. Без этого разрешения подсчет шагов работать не будет.

## 📊 Работа с данными

### Хранение данных:
- **SharedPreferences:** Хранение предыдущего значения шагов и даты
- **SQLite Database:** История шагов по дням
- **Таблица steps_history:**
    - `date` (TEXT) - дата в формате "dd.MM"
    - `steps` (INTEGER) - количество шагов

### Логика подсчета:
1. Приложение получает абсолютное значение шагов от датчика
2. Вычисляет разницу с предыдущим сохраненным значением
3. Добавляет сохраненные шаги за текущий день
4. Сохраняет общее количество в базу данных
5. При смене дня сбрасывает счетчик и сохраняет итог

## 🎨 Интерфейс

### Главный экран (HomeFragment):
- Большой счетчик шагов по центру
- Круглый фон для визуального выделения
- Долгое нажатие на счетчик для сброса

### Экран истории (HistoryFragment):
- Текстовая история шагов за 7 дней
- Гистограмма с визуализацией
- Названия дней недели и даты

## 🔄 Жизненный цикл

### Обработка событий:
- **onResume/onPause:** Регистрация/отмена регистрации слушателя датчика
- **onStart/onStop:** Управление ресурсами датчика
- **DayChanger:** Обработка смены даты

### Сохранение состояния:
- Данные сохраняются при выходе из приложения
- Автоматическое восстановление при повторном запуске
- Корректная обработка смены дня

## 🐛 Известные ограничения

1. На некоторых устройствах датчик шагов может отсутствовать
2. Для Android версий ниже Q разрешение не требуется, но может быть ограничена точность
3. При сбросе шагов обнуляется только текущий день
4. Гистограмма отображает только дни с ненулевым количеством шагов

## 📄 Лицензия

Это учебный проект, созданный для демонстрации работы с датчиками шагов и локальным хранением данных на Android.

---

**Примечание:** Для корректной работы приложения на реальном устройстве убедитесь, что датчик шагов поддерживается вашим устройством и разрешено отслеживание активности.